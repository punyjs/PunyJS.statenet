/**
* The listener is an API that exposes part of the listers manager, which can be used, externaly from the TruJS.state namespace, to add or remove listeners.
* @factory
*/
function _Listener (
    endpoint_listenerManager
) {

    /**
    * @worker
    */
    return Object.create(null, {
        "": {

        }
    });
}